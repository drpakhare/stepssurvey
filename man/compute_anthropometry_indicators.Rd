% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicators_anthropometry.R
\name{compute_anthropometry_indicators}
\alias{compute_anthropometry_indicators}
\title{Compute Anthropometry Indicators}
\usage{
compute_anthropometry_indicators(design)
}
\arguments{
\item{design}{A survey design object from \code{\link[=setup_survey_design]{setup_survey_design()}}.}
}
\value{
A named list of survey estimates. Each element contains estimates
(as tibble with columns: estimate, lower, upper, etc.) for:
\itemize{
\item \code{overweight_obese_total}: overweight or obese (BMI >=25), overall
\item \code{overweight_obese_by_sex}: overweight or obese, by sex
\item \code{overweight_obese_by_age}: overweight or obese, by age group
\item \code{obese_total}: obese (BMI >=30), overall
\item \code{obese_by_sex}: obese, by sex
\item \code{obese_by_age}: obese, by age group
\item \code{central_obesity_total}: central obesity, overall
\item \code{central_obesity_by_sex}: central obesity, by sex
\item \code{central_obesity_by_age}: central obesity, by age group
\item \code{bmi_mean_total}: mean BMI, overall
\item \code{bmi_mean_by_sex}: mean BMI, by sex
\item \code{waist_cm_mean_total}: mean waist circumference, overall
\item \code{waist_cm_mean_by_sex}: mean waist circumference, by sex
(if the corresponding variables exist in design)
}
}
\description{
Calculates prevalence of overweight, obesity, and central obesity, plus
mean BMI and waist circumference, from a survey design object.
}
\examples{
\donttest{
  test_data <- generate_test_data(n = 500, seed = 42)
  cols <- detect_steps_columns(test_data)
  clean <- clean_steps_data(test_data, cols)
  design <- setup_survey_design(clean)
  anthropometry_results <- compute_anthropometry_indicators(design)
}
}
\seealso{
\code{\link[=compute_all_indicators]{compute_all_indicators()}}
}
